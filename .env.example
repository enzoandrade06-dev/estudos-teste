# Banco de dados local (SQLite). Copie este arquivo para .env e ajuste se precisar.
# O arquivo .env e o próprio banco são ignorados pelo git — nunca commite nenhum dos dois.
DATABASE_URL="file:./dev.db"
